# Name this workflow descriptively
name: Promote iflow1

on:
  push:
    branches:
      - main
    paths:
      # ⬇️ THIS PATH MUST BE MANUALLY SET FOR EACH IFLOW WORKFLOW ⬇️
      - 'iflows/iflow1/**'
  workflow_dispatch:

jobs:
  build:
    # ... (The build job remains unchanged)
    name: 📦 Build Artifact
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Determine iFlow Name from Filename
        id: iflow_details
        run: |
          WORKFLOW_PATH="${{ github.workflow}}"
          FILENAME="${WORKFLOW_PATH##*/}"
          WITHOUT_PREFIX="${FILENAME#promote-}"
          IFLOW_ID="${WITHOUT_PREFIX%.yml}"
          echo "Determined iFlow ID for artifact: $IFLOW_ID $WORKFLOW_PATH"
          echo "id=$IFLOW_ID" >> "$GITHUB_OUTPUT"

      # ⬇️ 3. ARTIFACT now only contains the specific iFlow + global config ⬇️
      - name: Archive iFlow and Configs into an Artifact
        uses: actions/upload-artifact@v4
        with:
          name: iflow-artifact-${{ github.run_id }}
          path: |
            iflows/${{ steps.iflow_details.outputs.id }}
            config/
          retention-days: 7