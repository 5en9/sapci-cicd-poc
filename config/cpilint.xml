<?xml version="1.0"?>
<cpilint>
    <rules>
        <naming>
            <scheme>
                <regex>^[a-z0-9]+$</regex>
            </scheme>
            <message>Use only lowercase</message>
            <apply-to>iflow.id</apply-to>
        </naming>
    </rules>
</cpilint>